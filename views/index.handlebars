<h1>Burgers!</h1>

<h2>Burgers that are not devoured!</h2>

<ol>
  {{#each burgers}}
  {{#unless devoured}}
  <li>{{burger_name}}<button id={{burger_name}}>Devour</button></li>
  {{/unless}}
  {{/each}}
</ol>

<h2>Burgers that are devoured!</h2>

<ol>
  {{#each burgers}}
  {{#if devoured}}
  <li>{{burger_name}}</li>
  {{/if}}
  {{/each}}
</ol>

<h2>Add a Burger</h2>
<form id="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>
  <button type="submit">Add Burger</button>
</form>